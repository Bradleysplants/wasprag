# .env.server
# Contains secrets and server-side configurations.
# DO NOT COMMIT THIS FILE TO VERSION CONTROL!

# --- Required Wasp Server Vars ---

# The NER model to use for entity recognition
NER_MODEL_NAME="Dudeman523/NER-Bert-Based-Cased-PlantNames-Onnx"

# The URL of the PostgreSQL database
DATABASE_URL="postgresql://postgres:Lucian4635!!@[2601:7c0:c400:9650:d6be:d9ff:fed6:3a14]:5432/botanical_assistant"

# The URL your frontend is accessible at (used by server for redirects, email links etc.)
# Default for local dev is usually http://localhost:3000
WASP_WEB_CLIENT_URL="http://localhost:3000"

# The URL your backend server is accessible at (used for OAuth redirects etc.)
# Default for local dev is usually http://localhost:3001
WASP_SERVER_URL="http://localhost:3001"

# A strong, random secret for signing JWTs (at least 32 chars long)
# Generate one using: openssl rand -base64 32
JWT_SECRET="6af5da3783dba0b65b116374df2408446091aca06bc9d86838992a7b47e4040a4b0053ae63e07b7f9d318730333bbee1f76b0529185131e72153c8aa452def49"

# --- General App Configuration ---
# Node environment (useful for conditional logic in your code)
NODE_ENV=development

# --- Language Model Configuration ---
MODEL_NAME="bartowski/Meta-Llama-3.1-8B-Instruct-GGUF" # The model name to use for the language model
GGUF_MODEL_FILE_NAME="Meta-Llama-3.1-8B-Instruct-Q8_0.gguf" # The file name of the GGUF model
MODEL_TEMPERATURE="0.7" # Stored as string, coerce to number in code/validation
MODEL_MAX_TOKENS="1024" # Stored as string, coerce to number in code/validation
HUGGING_FACE_HUB_TOKEN="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"

# --- Embedding Model Configuration ---
EMBEDDING_MODEL="Xenova/bge-large-en-v1.5"

# Resend Email Configuration
RESEND_API_KEY="re_MUSZz9SF_MVayCyDru559TNygGhn67RpH"
RESEND_DEFAULT_FROM_EMAIL="admin@botani-buddy.us"

# --- API Keys ---
# OPENAI_API_KEY="your_openai_api_key_here" # Uncomment and set if you use OpenAI
HUGGINGFACE_API_KEY="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"
TREFLE_API_KEY="yKznQbWYxwFbFPG5PspKyIorAuRhagWlyxTGEobF9aY"

# --- Trefle API Configuration ---
TREFLE_API_BASE_URL="https://trefle.io/api/v1"
TREFLE_RATE_LIMIT_MAX="120"   # Stored as string, coerce to number
TREFLE_RATE_LIMIT_WINDOW="3600" # Stored as string, coerce to number
TREFLE_CACHE_TTL="172800"     # Stored as string, coerce to number
TREFLE_MAX_RETRIES="3"          # Stored as string, coerce to number
TREFLE_RETRY_DELAY_MS="500ms"         # Stored as string, coerce to number

# --- RAG Configuration ---
RAG_TOP_K="5"                   # Stored as string, coerce to number
RAG_SIMILARITY_THRESHOLD="0.7"  # Stored as string, coerce to number
RAG_CHUNK_SIZE="512"            # Stored as string, coerce to number
RAG_CHUNK_OVERLAP="50"          # Stored as string, coerce to number
